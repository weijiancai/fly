package com.fly.db.util;

import org.junit.Test;

/**
 * @author weijiancai
 */
public class SqlFileLoaderTest {
    @Test
    public void testExecute() {
        //SqlFileLoader loader = new SqlFileLoader(this.getClass().getResource("/crm_workflow.sql"));
        /*String[] sqlStatements = loader.getSqlStatements();
        for (String sql : sqlStatements) {
            System.out.println(sql);
        }*/

        //loader.execute();
        //DBManager manager = new DBManager();
//        new DBCompare("crm20120303", "crm");
    }
}
